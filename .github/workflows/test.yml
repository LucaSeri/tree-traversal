name: 🚀 Test 

on:
  pull_request:
    types: [opened, reopened]

jobs:
  run:
    name: 🚀 Test 
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🚀 Test 
        shell: bash
        run: pytest test.py
